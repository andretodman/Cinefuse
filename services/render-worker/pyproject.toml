[project]
name = "cinefuse-render-worker"
version = "0.0.0"
description = "Cinefuse M0 render worker stub"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28.1",
  "redis>=6.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
